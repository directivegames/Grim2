---
description: Use Genesys MCP only when Cursor exposes genesys tools; never probe MCP via shell
alwaysApply: true
---

# Genesys MCP (live editor)

## Availability gate (required)

Treat Genesys MCP as **off** unless **this chat session** exposes Genesys MCP tools in the tool list (e.g. `query_scene`, `query_editor`, `batch_execute`).

- **Do not** assume MCP is available because `.cursor/mcp.json` exists, because the user mentioned MCP, or because a skill references MCP.
- **Do not** call the MCP HTTP endpoint, `curl`, `Invoke-WebRequest`, or any shell workaround to talk to `127.0.0.1` / the URL in `mcp.json`.
- If the user says MCP is disabled or not connected, treat MCP as off for the rest of the session unless they say it is enabled again.

When MCP is off:

1. Say briefly that live editor access needs Genesys MCP enabled and connected in Cursor (and the editor running with this project).
2. Continue with code, filesystem, and repo tools where possible.
3. Do **not** open `*.genesys-scene` files unless the user explicitly asks (see `AGENTS.md`).
4. Do **not** retry MCP in the same turn.

## When MCP is on

- Follow `.agents/skills/genesys/genesys-mcp-orchestrator/SKILL.md`.
- Use only the provided Genesys MCP tools — not HTTP or terminal emulation.
- Call `query_*` and `action_*` tools directly — no preamble discovery call. Use `batch_execute(mode="readOnly")` for inspection and `dryRun` → `batch_execute(apply, approval={summary})` for edits. Pass inline `approval={summary}` on `batch_execute` or direct `action_*` calls — no separate `genesys_request_approval` needed in `auto` mode.
- Check `query_editor(getState)` for connectivity/readiness and `query_editor(getBusyState)` before mutating; do not edit during play mode or long builds.

### Asset paths (MCP vs filesystem)

- MCP returns project-relative forward-slash paths (e.g. `assets/default.genesys-scene`); `assetPath` may also use `@project/...`.
- When comparing MCP results to repo search or glob output, treat `assets/foo` and `.\assets\foo` as the same asset — not a mismatch.
- For runtime references in game code, use `@project/...` per `AGENTS.md`.
