/**
 * Previously patched ENGINE.SceneNode.prototype.getMovementPredictor.
 * That shim now lives on {@link GameRootNode} — keep this module so existing
 * imports (game.ts / auto-imports) resolve, but do nothing at load time.
 */
void 0;
